name: Build
on:
  push:
    branches:
      - main
jobs:
  bulid-mac:
    runs-on: macos-12
    steps:
      - name: Checkout repository and submodules
        uses: actions/checkout@v3
        with:
          submodules: recursive

      - name: Build
        run: |
          python3 build.py
